# Deliverance Church Utawala

## Project structure

- `backend/` — Django + Django REST Framework API (MySQL/cPanel-friendly)
- `frontend/` — Vite + React frontend (all UI code under `frontend/src/`)

## Notes

- The repo previously included a Next.js + Prisma implementation; some legacy frontend files may still reference `next/*` shims.
